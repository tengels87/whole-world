using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class BuffTest : MonoBehaviour {

	void Start() {
		
	}

	void Update() {
		/*
		if (Input.GetKeyDown(KeyCode.Return)) {
			Buff buff = this.gameObject.GetComponent<Buff>();
			buff.init();
		}
		*/
	}
}